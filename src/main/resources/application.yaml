spring:
  application:
    name: redesolidaria
  datasource:
    url: jdbc:postgresql://localhost:5432/redesolidaria_spring
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    # driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      "[hibernate.format_sql]": true